# MiniMaxRefDirector-ComfyUI 依赖
# 以下包在 ComfyUI 中已自带，无需额外安装：
#   torch, numpy, Pillow, typing_extensions

# 音频/视频加载（lib.py 使用 av 读取音频）
av>=12.0.0

# 音频序列化为临时 WAV（lib/audio.py save_audio_to_temp_wav；
# 注：torchaudio 的 wav 写出后端同样依赖 soundfile）
soundfile>=0.12.1
